// ZAD 3 //ndk

#include <stdio.h>
#include <stdlib.h>

int main()
{
    FILE *fp;
    FILE *fp2;
    fp=fopen("plik1.txt", "w");

    int 1kolumna=0;
    int 2kolumna=0;

    int tresc = getc (fp);
    while(tresc != EOF )
    {

    }
}

/* tab

int sum1=0,sum2=0;
i=0;
while(fscanf(fp, "%i", &liczba) != EOF;
    if(i%2==0)
    {

    }

*/

// poczytać o plikach binarnych, flagach binarnych
// funkcje fread fwrite fseek

// ZAD 4 //1335

//im

}
printf("\n");
i=0;
}
for(i=0;i<n:i++)
  printf("%i",suma[i]);
